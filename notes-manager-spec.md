# Project Spec — moved to the vault

**This file is no longer the spec.** The canonical version is a note in the Cortex vault:

- Vault: `../cortex-notes`
- Note: `notes/project-spec.md` — "Project Spec"
- Note id: `d84fe0e6-ac70-4fb6-b661-ad4ea580d66f`
- Former title: "Notes Manager Spec" (kept in the note's `aliases`)

Read it through the `cortex` MCP server (`get_note`, then `get_section` for the part you
need) rather than opening a file. Edit it with `patch_section` using the current section
revision.

The vault is the source of truth for project documentation, so this pointer is kept
deliberately short: any prose added back here would be a duplicate that silently drifts
from the real spec.
